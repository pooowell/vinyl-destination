# Spotify API Credentials
# Get these from https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret

# Discogs API Token
# Get this from https://www.discogs.com/settings/developers
DISCOGS_TOKEN=your_discogs_token

# Session encryption secret (generate a random string)
# You can generate one with: openssl rand -base64 32
NEXTAUTH_SECRET=your_random_secret_string

# Base URL for OAuth redirects
# Use 127.0.0.1 (not localhost) for Spotify OAuth compatibility
NEXT_PUBLIC_BASE_URL=http://127.0.0.1:3000

# SQLite database path (default: ./data/vinyl.db)
# DATABASE_PATH=./data/vinyl.db
